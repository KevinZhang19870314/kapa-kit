The previous task is now complete. Execute the GAPA evaluation process (skip the entire process if it was a simple Q&A, casual chat, or an IDE automated flow such as spec run-all-tasks execution):

First read {{gapaDir}}/gapa-rules.md to get the evaluation rules, then:

1. **Task evaluation:** Evaluate against the 5 dimensions in gapa-rules.md, append results to {{gapaDir}}/memory.md (include "Extract skill" and "Preference update" fields)
2. **Workflow extraction:** If the workflow from this task has reuse value, extract it as {{gapaDir}}/skills/{name}.md
3. **Preference update:** If new user preferences or habits are observed, update {{gapaDir}}/preferences.md

Evaluate honestly and specifically, action items must be actionable, be conservative with skill extraction.
